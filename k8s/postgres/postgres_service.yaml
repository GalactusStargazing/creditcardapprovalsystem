apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: credit-card-app
  labels:
    app: postgres
spec:
  type: clusterIP
  selector:
    app: postgres
  ports:
      - port: 5432
        targetPort: 5432
        protocol: TCP
