apiVersion: v1
kind: Service
metadata:
  name: application
  namespace: credit-card-app
  labels:
    app: application
spec:
  selector:
    app: application
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
