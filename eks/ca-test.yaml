apiVersion: v1
kind: Pod
metadata:
  name: ca-test
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      resources:
        requests:
          cpu: 600m
          memory: 128Mi
        limits:
          cpu: 600m
          memory: 128Mi