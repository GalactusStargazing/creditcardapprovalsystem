apiVersion: v1
kind: Pod
metadata:
  name: ca-test
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      resources:
        requests:
          cpu: 700m
          memory: 128Mi
        limits:
          cpu: 700m
          memory: 128Mi