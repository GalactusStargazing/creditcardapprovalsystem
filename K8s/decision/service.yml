apiVersion: v1
kind: Service
metadata:
  name: credit-decision
  namespace: credit-card

spec:
  type: ClusterIP

  selector:
    app: credit-decision

  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP